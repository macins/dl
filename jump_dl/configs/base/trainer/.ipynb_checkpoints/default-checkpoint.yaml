trainer:
  use_amp: true
  amp_dtype: bfloat16
  num_epochs: 60
  device: cuda
  monitor_key: cosine_similarity
  monitor_mode: max
  grad_clip_norm: 10.0
  ema:
    enabled: true
    decay: 0.998
    start_step: 500
    eval_with_ema: true
    save_ema_checkpoint: true
